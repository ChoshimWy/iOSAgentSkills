# Deep Links 与 URL 路由

## 用途
用于把外部 URL、推送点击或系统入口映射到应用内部路由。

## 推荐做法
- 先把 URL 解析成内部 `Route`。
- 再由根 router 或 tab router 统一分发。
- 避免在叶子视图里直接解析 URL。

## 规则
- 路由类型要 `Hashable`、可测试、可序列化。
- 未知路径要安全降级，不能让应用进入非法状态。
