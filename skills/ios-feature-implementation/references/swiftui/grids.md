# 网格布局

## 用途
用于媒体画廊、图标网格、卡片墙和自适应平铺布局。

## 核心规则
- 优先使用 `LazyVGrid` / `LazyHGrid`。
- 列数和单元宽度根据内容密度决定，不要固定到所有设备都一样。
- 每个单元的 identity 必须稳定。

## 建议
- 图标或媒体优先使用 adaptive column。
- 大图或复杂 cell 需要关注滚动性能和图片下采样。
