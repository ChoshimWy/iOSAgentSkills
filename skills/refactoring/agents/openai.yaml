interface:
  display_name: "代码重构"
  short_description: "处理长方法、重复逻辑、深层嵌套与通用结构重构流程要求"
  default_prompt: "使用 $refactoring 处理长方法、重复代码、深层嵌套、回调地狱和 God Object 等通用代码异味重构；若任务产出修改了 Apple Xcode 项目相关内容，默认以定向测试/必要验证与独立 reviewer subAgent 执行的 $code-review 放行为收口；$final-evidence-gate / $verify-ios-build 仅在用户显式要求或需要补强完整项目环境证据时按需使用。"
