# 列表与 Section

## 用途
用于 feed、设置列表、可滚动分组内容和系统风格清单。

## 核心规则
- 列表 identity 必须稳定。
- 行内容保持轻量，昂贵计算移出 `body`。
- 视觉上属于同一组的信息用 `Section` 组织。

## 建议
- 简单信息展示优先 `List`，完全自定义布局再考虑 `ScrollView + LazyVStack`。
- 如果行很多且频繁更新，优先检查性能和观察范围。
