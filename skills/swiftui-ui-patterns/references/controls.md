# 常用控件

## 用途
为设置和数据输入选择合适的 `Toggle`、`Picker`、`Slider`、`Stepper` 等原生控件。

## 核心规则
- 状态简单时优先原生控件，不要先做自定义组件。
- 标签、说明文字和禁用态要完整。
- 控件状态归属要清晰，父子传值用 `Binding`。

## 建议
- `Toggle` 适合开关型设置。
- `Picker` 适合有限离散选项。
- `Slider` 适合连续数值。
- `Stepper` 适合小范围递增递减。
