# Verification Artifact Schema

`agent-summary.json` is the first artifact Agents should read after any `codex_verify` / build-queue verification. It merges queue job metadata with the final verification report so Agents can avoid reading queue output, process listings, command logs, or raw `xcodebuild` logs. `verification-report.json` remains the detailed report generated by local scripts from raw logs; raw logs remain secondary evidence by path only.

## Agent Summary Required Fields

```json
{
  "schema_version": 1,
  "producer": "codex_verify_agent_summary",
  "status": "passed | failed | blocked | unknown",
  "verification_level": "unit | build | ui | full",
  "route": "codex_verify -> build-queue daemon -> xcodebuild",
  "repo_root": "/path/to/repo",
  "workspace_or_project": "App.xcworkspace",
  "project_selection": {
    "type": "workspace | project",
    "value": "App.xcworkspace",
    "source": ".codex/xcodebuild.env | auto_discovered | xcodebuild-args-or-env",
    "reason": ".xcworkspace preferred over .xcodeproj",
    "workspace_candidates": ["App.xcworkspace"],
    "project_candidates": ["App.xcodeproj"]
  },
  "scheme": "App",
	  "scheme_selection": {
    "scheme": "App",
    "source": ".codex/xcodebuild.env | auto_discovered | xcodebuild-args-or-env",
    "reason": "scheme has unit test binding",
    "testables": ["AppTests"],
    "has_unit_tests": true,
    "has_ui_tests": false,
    "scheme_path": "App.xcodeproj/xcshareddata/xcschemes/App.xcscheme",
	    "candidate_schemes": ["App"]
	  },
	  "environment_sanity": {
	    "cwd": "/path/to/repo",
	    "workspace": {
	      "configured_value": "App.xcworkspace",
	      "absolute_path": "/path/to/repo/App.xcworkspace",
	      "contents_xcworkspacedata_exists": true,
	      "usable_by_cli_sanity": true
	    },
	    "project": null
	  },
	  "configuration": "Debug",
  "action": "test",
  "destination": {
    "type": "physical_device | simulator | generic_ios | macos | unknown",
    "value": "id=00008110-001234567890001E",
    "selected_device_reason": "using explicit device identifier"
  },
  "only_testing": [
    "AppTests/FooTests/test_bar"
  ],
  "executed_command": "xcodebuild -workspace App.xcworkspace ...",
  "executed_commands": [
    "xcodebuild -workspace App.xcworkspace ..."
  ],
  "queue_job_id": "20260617-...",
  "queue_job_dir": "/tmp/codex-build-queue/jobs/20260617-...",
  "fingerprint": "short stable fingerprint",
  "cached": false,
  "summary": "one concise sentence",
  "first_blocking_error": null,
  "failed_tests": [],
  "warnings_count": 0,
  "ui_smoke": {
    "executed": false,
    "result": "skipped",
    "message": "not evaluated"
  },
  "artifact_paths": {
    "agent_summary": ".../agent-summary.json",
    "verification_report": ".../verification-report.json",
    "diagnostics_json": ".../diagnostics.json",
    "build_summary": ".../build-summary.txt",
    "raw_log": ".../job.log"
  },
  "raw_log_policy": "forbidden_by_default",
  "needs_raw_log": false,
  "next_action": "fix_first_error | inspect_environment | blocked | none"
}
```

`queue_job_id` and `queue_job_dir` are always present in `agent-summary.json`; they are `null` for standalone `build_check.py` runs that did not enter the build-queue wrapper.

## Verification Report Required Fields

```json
{
  "schema_version": 1,
  "producer": "codex_verify",
  "parser": "builtin-digest-parser",
  "formatter": "xcbeautify | xcpretty | xcprint | null",
  "status": "passed | failed | blocked | unknown",
  "mode": "auto | build | unit | ui | full | xcodebuild | build-check",
  "fingerprint": "short stable log or verification fingerprint",
  "cached": false,
  "summary": "one concise sentence",
  "first_blocking_error": null,
  "failed_tests": [],
  "warnings_count": 0,
  "artifact_paths": {
    "agent_summary": ".../agent-summary.json",
    "verification_report": ".../verification-report.json",
    "diagnostics_json": ".../diagnostics.json",
    "build_summary": ".../build-summary.txt",
    "test_summary": ".../test-summary.json",
    "xcresult_summary": ".../xcresult-summary.json",
    "formatted_log": ".../formatted-build.log",
    "source_context": ".../source-context.txt",
    "raw_log": ".../build.log"
  },
  "tool_bootstrap": {
    "formatter": "xcbeautify | xcpretty | xcprint | null",
    "status": "available | installed | fallback_builtin_parser | required_formatter_unavailable | missing_install_disabled | disabled | dry_run",
    "install_attempted": false,
    "install_succeeded": false,
    "install_command": null,
    "message": "script-owned formatter bootstrap summary"
  },
  "baseline": {
    "workspace_or_project": "App.xcworkspace",
    "scheme": "App",
    "configuration": "Debug",
    "action": "build",
	    "destination": "platform=iOS Simulator,id=<selected-simulator-id>",
    "destination_type": "simulator",
    "derived_data": "Xcode default"
  },
  "project_selection": {
    "type": "workspace",
    "value": "App.xcworkspace",
    "source": "auto_discovered",
    "reason": ".xcworkspace preferred over .xcodeproj"
  },
	  "scheme_selection": {
    "scheme": "App",
    "source": "auto_discovered",
    "reason": "scheme has unit test binding",
    "testables": ["AppTests"],
    "has_unit_tests": true,
	    "has_ui_tests": false
	  },
	  "environment_sanity": {
	    "cwd": "/path/to/repo",
	    "workspace": {
	      "absolute_path": "/path/to/repo/App.xcworkspace",
	      "contents_xcworkspacedata_exists": true,
	      "usable_by_cli_sanity": true
	    }
	  },
	  "only_testing": [],
  "executed_commands": [],
  "suggested_next_action": "fix_first_error | inspect_environment | blocked | none",
  "raw_log_policy": "forbidden_by_default",
  "needs_raw_log": false
}
```

## Agent Reading Order

1. Read `agent-summary.json`.
2. If `first_blocking_error` is present, inspect only the targeted source location or test selector.
3. Read `source-context.txt` only when the first error points to a source line and the snippet is enough.
4. Read `verification-report.json` or `diagnostics.json` only when the summary is incomplete.
5. Read `build-summary.txt` only when additional local context is required.
6. Read `formatted-build.log` only when the report says formatter output is needed.
7. Read raw log snippets only when `needs_raw_log=true` or the user explicitly asks.

## Tooling Contract

- The wrapper / digest script owns `xcbeautify`, `xcpretty`, `xcprint`, `xcresulttool`, or similar tooling.
- If the chosen formatter is missing, the script attempts installation and records the result in `tool_bootstrap`.
- Agents must not run package-manager installs or formatter commands manually.
- Formatter output is intermediate evidence; `parser` identifies the digest parser and `formatter` records the optional external formatter used before digesting. `agent-summary.json` is the Agent contract; `verification-report.json` remains the detailed verification report.
- The real `xcodebuild` exit code must be preserved even when formatter parsing succeeds.
- Workspace path, unavailable destination, `CoreSimulator`, `simdiskimaged`, and `CoreDeviceService` blockers should be reported as `status=blocked`, `failure_domain=env_issue`, and `suggested_next_action=inspect_environment` rather than as XCTest failures.
